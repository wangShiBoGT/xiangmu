# 批次 5 验收记录 · 宪法清理（V-1 / W-4 / DS0 / DS1 / DS5）

日期：2026-07-24 ｜ 基线 commit：`181a34f` ｜ 依据：《合规速查表》（不整卷加载宪法）

## 一、范围与交付

| 项 | 内容 | 状态 |
|---|---|---|
| V-1 | `ChatMessage` 违宪文案「深度思考中/已深度思考」→「推理段·生成中 N 秒 / 推理段（<think> 输出）」，注释同步去拟人 | ✅ |
| DS1 | 全站 34 处内联 SVG 图标并入唯一图标库 `icons.tsx`（24 viewBox / 1.5 stroke / round caps）；新增 20 个语义图标；图表类 5 处按规保留内联 | ✅ |
| W-4 | 拟人词复查：`models.ts` 描述、`trace.ts` 控制 token 标签、`ModelProfile.tsx` 字段名 中的「思考」→「推理链/推理段」 | ✅ |
| DS0 | `index.css` 固化 `--motion-*`/`--radius-*` 事实源；两处硬编码 0.15s 过渡改引 token；产出 `docs/DS0-token对照表.md` 校准记录 | ✅ |
| DS5 | 快照防线新增第 6 屏（助手推理段胶囊 + 复制/重新生成图标），锁定 V-1 文案与 DS1 图标 | ✅ |

## 二、改动文件（17）
`src/App.tsx` · `src/components/ChatMessage.tsx` · `DiscoverPage.tsx` · `HistoryDrawer.tsx` · `LandingHero.tsx` · `ModelProfile.tsx` · `ModelSelect.tsx` · `ObservePage.tsx` · `Sidebar.tsx` · `icons.tsx` · `dsSnapshots.test.tsx`(+snapshot) · `src/index.css` · `src/lib/models.ts` · `trace.ts` · `trace.test.ts` · `docs/DS0-token对照表.md` · `docs/批次5-验收记录.md`

## 三、验证（代码层，无截图/视频）
- `tsc -b`：0 error
- `oxlint`：0 warning / 0 error
- `vitest run`：**192 passed**（30 文件）；DS5 快照按预期变更 3 处（Sidebar 光圈标准化、ModelProfile 推理链输出、新增第 6 屏），均属本批次有意改动，已留痕（宪法第六章）
- `vite build`：成功（chunk >500kB 警告为既有，非本批次引入）
- `loadtest`（preview 4173，autocannon）：并发 50 → 157316 req / 2xx=157316 / **错误 0** / p99 9ms；并发 200 → 144612 req / 2xx=144612 / **错误 0** / p99 27ms

## 四、宪法自查（提交前 6 项）
1. **禁词**：用户可见文案已无「深度思考/思考中/正在理解/聪明/强大」等；`worker.ts` 的 `"请用中文思考和回答"` 为发给模型的**功能性 system prompt**（非产品对用户话术），改动会影响模型输出语言，按「以盲目修改为耻」保留。测试名/夹具中的「思考」为内部描述，保留。
2. **Evidence First**：未新增任何无来源的结论性话术；「犹豫点」仍为可展开到真实 topk 概率的索引标签（既有设计，未破坏）。
3. **五段式**：本批次为清理/固化，未改动任一屏的 问题→证据→交互→结论→回放 结构。
4. **图标规范**：单库、24 viewBox、1.5 stroke、round caps；Outline/Filled 语义（`IconStar` filled 表选中）；图表 SVG 不算图标，保留内联。
5. **动效红线**：未引入 spring/bounce、数字翻转、假进度；新 token 三档 150/300/600 已文档化，连续环境动画登记豁免。
6. **修改留痕**：快照变更原因见本记录第三节；token 校准与未做项见 `DS0-token对照表.md`。

## 五、已知限制 / 待确认债务（未擅自执行，需人类拍板）
- **字号收敛**：项目现存 14 档字号，宪法定 7 档。收敛会改变全站排版观感并推翻大量快照，属「重设计」，登记待确认（映射建议见对照表 §3）。
- **圆角 12px（rounded-xl）**：宪法定 8/16/full，项目大量使用 12px 中间档。收敛会改观感，登记待确认，本批次保留为已登记校准值。
- **模型描述营销级形容词**（「能力最强/质量最高/英文能力强」）：非拟人词，属语气/Evidence 边界问题，未在 W-4 范围内擅改，登记为后续可选清理。
- **浮层动效 180/240ms**：与宪法 150/300 略有偏差，属手感定制，登记为可选收敛。
