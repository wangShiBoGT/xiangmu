# Agent 3D 可视化规划 - 决策剧场

> **核心使命**：用高科技的 3D 可视化，让普通人**看懂** AI 的思考和决策过程  
> **设计原则**：科幻感 + 直观隐喻 + 渐进式深入

---

## 🎯 第一性原理检验

### 普通用户需要看到什么？

❌ **不需要**：
- 神经网络权重矩阵
- Token embedding 向量
- Softmax 概率计算
- 复杂的技术术语

✅ **需要看到**：
1. **AI 在"思考"** - 像人一样在想问题
2. **AI 在"选择"** - 多个备选答案中挑选
3. **AI 有"犹豫"** - 不确定时会纠结
4. **进度感** - 还剩多少、快完成了
5. **可信度** - 这个答案靠谱吗？

### 设计验证标准

**如果一个 60 岁不懂技术的人看 30 秒，能回答：**
- Q: AI 在做什么？
- A: "在想答案，还在挑选最好的词"

**通过 ✅** | **不通过 ❌**

---

## 🎭 核心概念：决策剧场（Decision Theater）

### 为什么是"剧场"？

**隐喻直观**：
- 人人都看过选秀节目（好声音、达人秀）
- "候选者上台 → 评委打分 → 选出最佳" 这个流程深入人心
- 舞台聚光灯 = AI 的注意力
- 掌声/喝彩 = 高概率选择

**符合第一性原理**：
- 展示"AI 在选择"（核心差异化）
- 可视化"候选 token"（不是黑盒）
- 直观显示"概率"（评委打分）

---

## 🎨 视觉设计

### 场景 1：思考舞台（Thinking Stage）

**3D 布局**：
```
         [幕布：正在思考...]
              ↓
    ┌─────────────────────────┐
    │     💭 想法云团         │  ← 漂浮的文字片段
    │   "技术角度" "用户角度" │
    │   "简单解释" "深入分析" │
    └─────────────────────────┘
         ↓ 筛选动画
    ┌─────────────────────────┐
    │    ✨ 选中："技术角度"  │
    └─────────────────────────┘
```

**普通人看到**：
- 多个"想法泡泡"在空中漂浮
- AI 在"思考"选择哪个角度
- 最终一个泡泡被高亮、放大

**动画**：
- 泡泡轻微摇晃（表示"思考中"）
- 被选中的泡泡发光 + 弹出动画
- 其他泡泡淡出

---

### 场景 2：词语选秀（Token Audition）

**3D 布局**：
```
          [舞台中央聚光灯]
               ↓
        ┌─────────────┐
        │   "首先"    │ ← 当前正在考虑的词
        └─────────────┘
             ↓
    候选者排队（左右两侧）：
    
    左侧排队：              右侧排队：
    🥇 "首先" 87%          🥈 "其次" 10%
    🥉 "第一" 2%           ⭐ "首要" 1%
    
    [评委席：概率柱状图]
    ████████░░ 87%  ← 金色发光
    ████░░░░░░ 10%
    ██░░░░░░░░ 2%
    █░░░░░░░░░ 1%
```

**普通人看到**：
- 像选秀节目，多个"候选词"排队
- 每个词头上有"得分"（概率）
- 聚光灯照在得分最高的词上
- 被选中后，词"走上舞台中央"

**动画**：
- 候选词轻微呼吸动画（表示"等待"）
- 得分高的词发金光、得分低的发暗光
- 选中瞬间：聚光灯扫射 + 掌声粒子特效
- 被选中的词飞向"答案区"

---

### 场景 3：答案拼图（Answer Assembly）

**3D 布局**：
```
    [已完成的部分]
    ┌─────────────────────────┐
    │ "首先，我们需要理解..." │ ← 深度雾化（已确定）
    └─────────────────────────┘
    
    [正在生成]
    ┌─────────────────────────┐
    │ "技术"  [?]  [?]  [?]   │ ← 当前词发光，后面模糊
    └─────────────────────────┘
    
    [进度条]
    ████████░░░░░░░░░░ 40%
    预计还需 12 秒
```

**普通人看到**：
- 答案像"拼图"一样一块块拼起来
- 已完成的部分变暗（确定了）
- 当前正在拼的"发光"
- 进度条显示还剩多少

**动画**：
- 新词"飞入"拼图位置
- 飞入瞬间：粒子爆炸特效
- 拼好后：轻微脉冲 → 淡化为背景

---

### 场景 4：可信度仪表（Confidence Meter）

**3D 布局**：
```
    [舞台右侧 - 可信度仪表盘]
    
         AI 确定度
    ┌─────────────────┐
    │   🟢 很确定     │ ← 绿色发光
    │   🟡 一般       │
    │   🔴 不确定     │
    └─────────────────┘
         ↑
      [指针动画]
    
    当前：很确定 (87%)
```

**普通人看到**：
- 像汽车的油表 / 速度表
- 指针指向"很确定"就安心
- 指针指向"不确定"要留意

**动画**：
- 指针平滑移动
- 指针位置决定背景色（绿/黄/红渐变）
- 不确定时：仪表盘轻微抖动

---

## 🎬 完整流程演示（30 秒体验）

### 时间轴

**0-5s：问题进入**
```
用户问题从屏幕底部"飞入"舞台
问题悬浮在舞台上方
舞台灯光亮起
```

**5-10s：思考阶段**
```
多个"想法泡泡"从舞台地板升起
泡泡在空中漂浮、碰撞、融合
AI 旁白："让我想想..."
最终一个泡泡被选中、放大、发光
```

**10-25s：生成阶段（循环）**
```
每 0.5 秒一个词：
1. 候选词从舞台两侧出现
2. 评委席显示概率柱状图
3. 聚光灯照向最高分的词
4. 选中词飞向答案区 + 粒子特效
5. 可信度仪表盘指针移动

同时：
- 进度条前进
- 已完成部分淡化为背景
```

**25-30s：完成**
```
最后一个词拼入
舞台灯光闪烁
答案区整体发光 → 淡出
展示完整答案
```

---

## 🎮 交互设计

### 普通用户模式（默认）

**只看"表演"**：
- 自动播放动画
- 人话旁白（"AI 在思考..."、"找到了！"）
- 简化显示（只看聚光灯和选中的词）

**互动**：
- 点击"想法泡泡"：显示"AI 为什么考虑这个"
- 点击"候选词"：显示"为什么这个词得分高"
- 点击进度条：快进 / 慢放

---

### 专业用户模式（可切换）

**深入细节**：
- 显示所有候选词（Top 10）
- 显示准确概率值（87.3%）
- 显示技术指标（entropy、logit）
- 可导出每一步的 trace

**互动**：
- 点击任意 token：弹出 TokenDetailDrawer
- 拖动时间轴：查看任意时刻的决策
- 对比模式：并排显示不同参数的运行

---

## 🛠 技术实现

### 技术栈

**3D 引擎**：
```typescript
// React Three Fiber (基于 Three.js)
import { Canvas, useFrame } from '@react-three/fiber'
import { useSpring, animated } from '@react-spring/three'
import { Text3D, OrbitControls, Environment } from '@react-three/drei'
```

**优势**：
- 与 React 无缝集成
- 声明式 API（易维护）
- 性能优化（自动批处理）
- 生态丰富（Drei 提供现成组件）

**动画库**：
```typescript
// React Spring（物理动画）
import { useSpring, useTrail, useChain } from '@react-spring/three'

// 词语飞入动画
const spring = useSpring({
  from: { position: [-5, 0, 0], scale: 0 },
  to: { position: [0, 0, 0], scale: 1 },
  config: { tension: 200, friction: 20 }
})
```

**粒子特效**：
```typescript
// @react-three/postprocessing
import { EffectComposer, Bloom } from '@react-three/postprocessing'

// 发光效果
<EffectComposer>
  <Bloom intensity={1.5} luminanceThreshold={0.9} />
</EffectComposer>
```

---

### 性能优化

**问题**：3D 渲染 + Token 生成同时进行，可能卡顿

**解决**：
1. **Web Worker 分离**：
   ```typescript
   // 主线程：3D 渲染
   // Worker 线程：LLM 推理
   // 通信：每个 token 传递一次数据
   ```

2. **Level of Detail (LOD)**：
   ```typescript
   // 距离远 → 低多边形模型
   // 距离近 → 高精度模型
   <Lod distances={[0, 10, 20]}>
     <mesh>高精度</mesh>
     <mesh>中等</mesh>
     <mesh>低精度</mesh>
   </Lod>
   ```

3. **实例化渲染**：
   ```typescript
   // 候选词使用同一个 geometry
   <Instances limit={1000}>
     {candidates.map(c => (
       <Instance key={c.token} position={c.pos} />
     ))}
   </Instances>
   ```

4. **按需加载**：
   ```typescript
   // 用户切换到 3D 模式才加载
   const DecisionTheater = lazy(() => import('./DecisionTheater'))
   ```

---

## 📐 组件架构

```typescript
<DecisionTheater>
  {/* 舞台环境 */}
  <Stage>
    <Lights />
    <Floor />
    <Background />
  </Stage>
  
  {/* 思考阶段 */}
  {phase === 'thinking' && (
    <ThinkingStage thoughts={thoughts} />
  )}
  
  {/* 生成阶段 */}
  {phase === 'generating' && (
    <>
      <CandidateRow candidates={candidates} />
      <Spotlight target={selected} />
      <ScoreBoard scores={probabilities} />
      <AnswerAssembly tokens={generated} />
    </>
  )}
  
  {/* UI 覆盖层 */}
  <Html>
    <ProgressBar progress={progress} />
    <ConfidenceMeter confidence={confidence} />
    <Narrator text={narration} />
  </Html>
  
  {/* 相机控制 */}
  <OrbitControls 
    enablePan={false}
    maxPolarAngle={Math.PI / 2}
  />
</DecisionTheater>
```

---

## 🎯 开发计划（2 周 / 80h）

### Week 1: 核心场景（40h）

**Day 1-2: 基础搭建（16h）**
- [ ] 搭建 React Three Fiber 环境
- [ ] 创建舞台场景（地板、灯光、背景）
- [ ] 测试基础动画（词语飞入/飞出）

**Day 3-4: 思考舞台（16h）**
- [ ] 想法泡泡组件
- [ ] 泡泡漂浮动画
- [ ] 选中泡泡的聚光灯效果
- [ ] 与 `<think>` 标签数据集成

**Day 5: 候选词选秀（8h）**
- [ ] 候选词排队布局
- [ ] 概率柱状图（3D 柱子）
- [ ] 聚光灯扫射动画
- [ ] 选中词飞向答案区

---

### Week 2: 交互 + 优化（40h）

**Day 6-7: 答案拼图（16h）**
- [ ] 答案区布局
- [ ] 词语拼接动画
- [ ] 粒子爆炸特效
- [ ] 进度条同步

**Day 8: 可信度仪表（8h）**
- [ ] 仪表盘 3D 模型
- [ ] 指针动画
- [ ] 背景色渐变
- [ ] Entropy 数据映射

**Day 9: 交互优化（8h）**
- [ ] 点击候选词查看详情
- [ ] 拖动时间轴回放
- [ ] 普通 / 专业模式切换
- [ ] 键盘快捷键

**Day 10: 性能优化 + 打磨（8h）**
- [ ] LOD 优化
- [ ] 实例化渲染
- [ ] 按需加载
- [ ] 浏览器兼容性测试

---

## ✅ 验证标准

### 普通用户测试

**找 3 个不懂技术的人，让他们看 30 秒，然后问：**

1. Q: AI 在做什么？  
   A: ✅ "在想答案 / 在选词"  
   A: ❌ "不知道 / 在计算"

2. Q: 哪个词被选中了？  
   A: ✅ 能指出聚光灯照的那个  
   A: ❌ "看不出来"

3. Q: AI 现在确定吗？  
   A: ✅ 能看懂仪表盘指针  
   A: ❌ "不知道"

**通过率 > 80% 才算成功**

---

### 专业用户测试

**找 3 个 AI 开发者，让他们用 5 分钟，然后问：**

1. Q: 能看到候选 token 吗？  
   A: ✅ "能，而且能看到概率"

2. Q: 能导出 trace 吗？  
   A: ✅ "能点击任意 token 查看"

3. Q: 比现有工具好在哪？  
   A: ✅ "更直观 / 更有趣 / 更易分享"

---

## 🚀 上线后的效果

### 预期用户反馈

**普通用户**：
- "哇，原来 AI 是这么想的！"
- "像看选秀节目一样有趣"
- "终于看懂 AI 了"

**专业用户**：
- "比 Langfuse 的 trace 好看太多"
- "可以截屏分享给团队"
- "调试小模型终于有趣了"

**社交传播**：
- 截屏 / 录屏分享到 Twitter
- "这个本地 AI 可视化太酷了"
- 吸引更多人尝试

---

## 🎨 视觉参考

### 风格参考

**科幻电影 UI**：
- Jarvis（钢铁侠）- 全息投影感
- Her（她）- 柔和光晕、粒子特效
- Minority Report - 手势交互

**游戏 UI**：
- Cyberpunk 2077 - 霓虹发光
- Detroit: Become Human - 决策树可视化
- Hades - 希腊神话风格的数据展示

**现实产品**：
- Tesla 仪表盘 - 实时数据 + 动画
- Apple 发布会动画 - 流畅过渡
- Stripe 官网 - 渐变 + 玻璃态

---

## 🔗 相关文档

- [FIRST-PRINCIPLES.md](FIRST-PRINCIPLES.md) - 为什么要做这个
- [DEVELOPMENT-ROADMAP-V2.md](DEVELOPMENT-ROADMAP-V2.md) - 整体规划
- [VISUAL-REDESIGN-PLAN.md](VISUAL-REDESIGN-PLAN.md) - 2D 视觉升级

**优先级**：
- Phase 0-2 完成后再做 3D 可视化
- 确保 2D 界面已经"不吓人"
- 3D 作为"高级功能"，而非必需

---

## 💡 核心信念

> **3D 可视化不是装饰，是让 AI 从"黑盒"变成"玻璃盒"的工具。**  
> **普通人看懂了，才会信任；信任了，才会使用。**  
> **让 AI 变得透明，是我们存在的意义。**

---

**准备好了吗？等前面的基础功能完成后，我们就开始这个"决策剧场"！** 🎭✨
