# AI Microscope · Layout Plan（布局重构方案）

> 约束：只改布局（DOM 顺序、栅格、区块归位）。不改颜色、不改动画、不改组件内部。功能 100% 保留。
> 目标气质：实验室（一台仪器、一份标本、一份报告），不是后台/Dashboard/九宫格。

## 一、统一阅读顺序（每页一个视觉中心）

每个页面自上而下严格遵循：

```text
Hero        观察对象本身（标本）——唯一视觉中心
  ↓
Explain     它发生了什么（摘要/故事/团队）
  ↓
Interaction 你能对它做什么（操作/再跑/开关）
  ↓
Result      实验产物（分岔/对比/双结局）
  ↓
Advanced    专业镜头（思路地图/没走的路/时间线/检索）——专业视图才展开
  ↓
Debug       调试台（Debug 条/Workflow/Decision 卡）——最底层
```

规则：
1. 视觉中心唯一：Hero 区之外任何区块不得比 Hero 更大更亮；
2. 不允许多张卡并列抢注意力：并列栅格最多 2 列，默认单列竖排叙事；
3. 控制不散落：操作集中在 Interaction 带与底部记录台，不再穿插在内容中间。

## 二、逐页方案

### Observe（核心页，改动最大）

**现状问题**：完成态的阅读顺序是「摘要 → Debug 工具 → 故事 → 专业镜头 → 团队 → 检索 → **正文答案**」——观察对象（答案正文）被压到最底部，上方 7 个区块并列抢戏，像后台报表。

**重构后（完成态）**：
1. Hero：Prompt（问题）→ **TokenText 答案正文**（把握度着色，唯一视觉中心）+ 错误卡（如有）
2. Explain：CompleteSummary（收束统计）→ ObservationSummary（观察摘要）→ Story 入口/播放器 → Team 回看
3. Interaction：尾部操作带（专业视图/熵热力/概率地形/更多）+ 底部记录台与对比芯片（现有位置不动）
4. Result：双结局卡、分岔树（实验产物，紧随正文）
5. Advanced（专业视图才出现）：思路地图 → 没走的路 → Agent 时间线 → 检索决策卡
6. Debug（专业视图才出现，最底）：Debug 条 → Workflow 阶段条 → Decision 卡

**运行中**：主舞台（TeamFlow/正文流出）即 Hero，保持现有顺序；控制台条属 Interaction，仍随专业视图展开。

**演示回放**：结束后的思路地图/没走的路已在正文之后，顺序符合，不动。

### Workspace（工作台）
- `md:grid-cols-2` 双卡并列 → 单列竖排（Hero=当前实验 → Explain → 其余顺次向下）。

### Archive（档案）
- 卡片墙 `lg:grid-cols-3` 三列九宫格 → 最多 2 列（`sm:grid-cols-2`），保持卡内容不动；页面中心是「找到一次运行」。

### Landing（落地页）
- 特性条 `lg:grid-cols-4` 四卡并列 → 最多 2 列，纵向叙事优先；Hero（真实回答着色示例）保持唯一中心。

### 其余页（discover / findings / journey / benchmark / story）
- 已是单列叙事流（journey 四幕、story 对话式、findings 列表），符合阅读顺序，不动。

## 三、不做的事

- 不改任何颜色 token、语义色、主题；
- 不改任何动画/过渡；
- 不改组件内部结构与视觉（只挪它们在页面里的位置与栅格）;
- 不删功能：所有区块只是换位，条件渲染逻辑（proView、phase、数据在场性）原样保留。

## 四、验收

- 完成态从上到下扫一遍即读懂：问题 → 答案 → 发生了什么 → 能做什么 → 实验产物 → 专业 → 调试；
- 任何页面无 3 列以上卡片栅格；
- `npx tsc -b` / lint / vitest / build 全绿，浏览器实测阅读顺序。
