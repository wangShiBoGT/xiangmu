# 决策优先 v2 实现 · 设计决策卡

> 上游：`docs/决策优先-AI思维可视化-重构设计稿.md`（创始人已拍板「全部开发完」）。
> 本卡覆盖 S1–S6 全量实现的五闸与数据口径。

## 五闸

```text
□ AODL 六层栈：决策事件 = 认知层「从困惑到理解」的主载体 ✔
□ 宪法：新增 P26 已入 VolI 与合规速查表；全部文案过词表（无拟人断言）✔
□ Evidence First：每个新视觉元素的驱动字段见下表 ✔
□ 认知阶梯：人话决策日志（阶1）→ 展开证据（阶2）→ 点击跳步进 Sampling Inspector（阶3）✔
□ Q→E→I→C→R：「它为什么这么写」→ 真实事件+读数 → 点击跳步/展开 → 用户自行判断 → 事件可回放定位 ✔
```

## 交付清单（驱动字段全部真实）

| 交付 | 内容 | 驱动字段 |
|---|---|---|
| S1 `lib/decisions.ts`（+7 测试） | 决策事件引擎：掷硬币（top-2 差<5% 且 top1>5%，closeSteps 同口径）/ 温度改命（选中 rank>1）/ 想法很散（entropyLevel≥0.7，director.birthAt 同阈值）/ 卡住了（dt>均值×3）；必然步静默；maxPerKind=3 预算（按显著度截取，一直爆=没有爆） | `topk[]`、`prob`、`entropy`、`dt`、temperature |
| S1 `ActivityLog` 重构 | 「内心独白」→「它刚才做了什么 · 决策日志」：人话一句 + 展开显示放弃的候选与证据口径；点击跳步 | decisionEvents |
| S2 `InstrumentCluster` 可读性 | 候选标签加大（10.5→12px）+ 描边阴影 + 高低交错避让；当前 token 玻璃标签加大（12.5→15px）；hover 信息卡下沉为底部信息条（不再遮挡词）；高光冻结：聚焦时其余切片降亮 0.6→0.38 | 布局改动，无新数据口径 |
| S3 `BirthScene` 词表投票 | 外圈淡点 = 尾部质量（点数∝1-Σtopk，每 4% 一点上限 24，确定性圆周分布，无随机数）；说明行加「其余词表分摊 X%」与「不是查词典——整个词表按概率投票，胜者由采样抽出」 | `topk[]`、尾部质量 1-Σtopk |
| S3 落选词沉降 | 概率场落选候选随衰减沉到地平面下方淡出（被放弃的词沉下去） | `topk[].prob`（衰减节奏） |
| S4 检索舞台 | 检索真实完成后、首 token 到达前，主舞台展示命中/弃用文档（已送入上下文=绿、未采用=虚线暗）；数据 = 本次 RetrievalRecord；检索未发生时不渲染 | RAG trace（query/results/selected） |
| S6 慢速跟播 | OceanView 回放条加「慢速」开关（50ms→200ms/步） | 播放速率，无新数据口径 |

## 诚实缺席（P9）

- 注意力连线 / 隐藏状态色彩空间 / 语义聚类：主模型 ONNX 暂无导出，不做；升级路线 = `tools/export_attention_onnx.py`。
- 呼吸脉动 / 随机杂色烟雾（创始人纲领原文）：违反 P24/P25，以真实熵驱动的确定性流场替代（裁决见设计稿 §5.2）。

## 失效条件（P24）

- decisions.ts 阈值与 closeSteps/director 同源；若上游口径修订，此处必须同步（测试会锁住行为）。
- attention 导出落地后：S3 场景可升级出真实连线层，本卡「诚实缺席」条目作废。
