# AI Microscope · Design System（唯一事实源）

> 上位法：`design-language.md`（判据：每一个像素要么承载证据，要么保持沉默）。
> 效力：**整个项目所有页面自此必须遵守本文件**。任何页面/组件出现本文之外的颜色、字号、圆角、阴影、动效，视为违规，评审一票否决。
> 本文只定规范，不含代码，不改现有页面。

禁令（全局，凌驾一切章节）：**不要 Glass（玻璃拟态/backdrop-blur 氛围）、不要渐变背景、不要彩虹色、不要 Glow（辉光/发光描边/彩色阴影）。**

---

## 1. Color Tokens

一个中性底 + 三个测量色。语义唯一：一个颜色只允许表达一种含义，反之一种含义只允许用一个颜色。

### 1.1 中性系（Lab 浅色 · 默认主题）

| Token | 值 | 语义 |
|---|---|---|
| `bg` | `#f6f6f4` | 页面工作台底 |
| `surface` | `#ffffff` | 部件面板底 |
| `surface-2` | `#f0f0ee` | 面板内嵌区（输入底/表头/代码底） |
| `ink` | `#1f2023` | 正文 |
| `ink-2` | `#6e7075` | 辅助文字 |
| `ink-3` | `#a4a6aa` | 弱提示/占位符 |
| `line` | `#e4e4e1` | 全站唯一边框色 |
| `line-strong` | `#c9c9c5` | 强调边框（聚焦表格线/轴线） |

### 1.2 中性系（Darkroom 暗室 · 可选主题）

深石墨，无蓝偏、无近黑：

| Token | 值 |
|---|---|
| `bg` | `#1b1c1e` |
| `surface` | `#242528` |
| `surface-2` | `#2d2e32` |
| `ink` | `#e8e8e6` |
| `ink-2` | `#9d9ea3` |
| `ink-3` | `#6b6c71` |
| `line` | `#37383c` |
| `line-strong` | `#4a4b50` |

### 1.3 测量色（全站仅此三个语义色）

| Token | 浅色值 / 暗室值 | 唯一语义 |
|---|---|---|
| `measure`（测量蓝） | `#2563eb` / `#5b8def` | 当前被测对象、选中、可交互、主动作 |
| `caution`（警示琥珀） | `#b45309` / `#d99a3d` | 高不确定性读数（高熵/犹豫/慢步） |
| `alert`（判定红） | `#b91c1c` / `#e05d5d` | 失败、报错、越界 |

配套弱底色：每个测量色有唯一一档 8–12% 透明度的同色弱底（`measure-wash` 等），用于选中态背景与徽章底。

规则：
- 成功/完成不设绿色：用中性 `ink` + ✓ 符号表达。
- 连续量（概率/熵）用单一测量色的**明度阶**（5 档），禁止多色梯度。
- 图表多系列对比用明度 + 线型（实/虚/点）区分，不加新颜色。
- 现有 indigo/emerald/rose/sky/cyan/violet 全部收编映射到上表。

---

## 2. Typography

### 2.1 字族（双字体制）

| 用途 | 字族 |
|---|---|
| 界面/正文 | Inter，回退系统栈（现有栈保留） |
| **一切读数**：数字、单位、token 原文、ID、字段名 | 等宽栈（ui-monospace/SF Mono/Menlo/Consolas）+ `tabular-nums`，无一例外 |

### 2.2 字阶（六档，全站唯一）

| 档 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| `display` | 24 / 32 | 600 | 页面唯一大标题（每屏 ≤1） |
| `title` | 16 / 24 | 600 | 区块标题 |
| `body` | 14 / 22 | 400 | 正文、解释句、按钮文字 |
| `reading` | 13 / 20 | 450 等宽 | 读数主档 |
| `label` | 12 / 16 | 500 | 轴标签、字段名、部件丝印（丝印可 +0.08em 字距大写） |
| `micro` | 11 / 14 | 400 | 单位、脚注（最小档，禁止更小） |

规则：任何 `text-[Npx]` 临时字号违规；9–10.5px 一律升档到 `micro`；32px 以上仅营销首屏 `display` 可放大至 32/40，产品内部禁止。

---

## 3. Spacing

8pt 网格，唯一间距序列：**4 / 8 / 12 / 16 / 24 / 32 / 48 / 64**（px）。

- 部件内边距：面板 16，紧凑行 8–12。
- 部件之间：同组 16，跨组 24–32。
- 禁止 6、10、14 等序列外值；禁止用空 div 撑空隙。

## 4. Radius（最多三种）

| Token | 值 | 用途 |
|---|---|---|
| `r-sm` | 6px | 控件：按钮、输入框、下拉、徽章、图表 chip |
| `r-md` | 10px | 容器：Card、Dialog、Tooltip、抽屉 |
| `r-full` | 9999px | 仅限：状态圆点、进度条端头 |

胶囊按钮废止（full 不再用于按钮）；现有 12/16px 圆角统一归档到 `r-md`。

## 5. Border（统一）

- 全站唯一描边：**1px solid `line`**。
- 唯一例外：表格轴线/聚焦分隔可用 `line-strong`，仍 1px。
- 禁止 2px 装饰描边、虚线装饰框（虚线仅保留一个语义：Empty State 投放区）、彩色描边（彩色只出现在 Focus Ring 与失败态，见后文）。

## 6. Shadow（最多两种）

| Token | 值 | 用途 |
|---|---|---|
| `shadow-raise` | `0 1px 2px rgb(0 0 0 / 0.06)` | 悬浮微起：Tooltip、下拉弹层 |
| `shadow-float` | `0 8px 24px rgb(0 0 0 / 0.12)` | 真正浮层：Dialog、抽屉 |

规则：**平面部件（Card、面板、按钮）一律无阴影**，层级靠边框与底色。彩色阴影 = Glow，禁止。

## 7. Animation / Motion（指针语法）

| Token | 时长/缓动 | 唯一用途 |
|---|---|---|
| `m-pointer` | 150ms ease-out | 数值变化、选中切换、hover 反馈、进度推进 |
| `m-expand` | 250ms ease-in-out | 展开/收起、下钻、抽屉/Dialog 进出 |
| `m-sampling` | 1.2s 循环 | **唯一合法循环动画**：「测量中」指示（单点明暗或细进度），同屏 ≤1 处 |

规则：
- 三 token 之外的时长/动画违规；600ms「场面」废止。
- 禁止：缩放弹跳、blur 入场、颜色脉冲、粒子、辉光呼吸。
- `prefers-reduced-motion`：`m-pointer`/`m-expand` 直切，`m-sampling` 静态。
- 位移入场幅度 ≤8px；透明度过渡允许。

## 8. Grid 与 Container Width

- 布局网格：12 列，gutter 16，页面左右安全边距 24（窄屏 16）。
- 部件按网格对齐；禁止部件自由漂浮与卡片套卡片。
- Container 宽度三档：
  - `container-reading` 720px：文档/故事/设置等阅读流；
  - `container-bench` 1200px：默认工作台（观察页主区 + 右栏）；
  - `container-full` 100%：仅全屏观测视图（3D/对比），仍保留 24 边距。
- 右栏（Inspector 栏）固定 320px；小于 1024px 时右栏下沉为抽屉。

## 9. Icon

- 唯一图标体系：线性、1.5px 描边、24×24 网格（渲染 16/20 两档），风格对齐 Lucide。
- 禁止 emoji 做界面图标（正文内容除外）、禁止实心/彩色/渐变图标。
- 图标颜色只允许 `ink-2`（默认）、`ink`（hover/激活）、三测量色（对应语义时）。
- 图标必配可访问名称（aria-label 或伴随文字）。

## 10. Button（仅四种）

| 变体 | 底 / 字 / 边框 | 用途 |
|---|---|---|
| **Primary** | `measure` 底 / 白字 / 无边框 | 每屏 ≤1 个主动作（开始记录/运行） |
| **Secondary** | `surface` 底 / `ink` 字 / 1px `line` | 常规动作 |
| **Ghost** | 透明底 / `ink-2` 字 / 无边框 | 低强调、工具行内联动作 |
| **Danger** | 透明底 / `alert` 字 / 1px `alert` 30% 边框 | 删除/中止等破坏性动作（确认后执行） |

统一规格：高度两档 32（默认）/ 28（紧凑）；圆角 `r-sm`；内边距 12–16；字号 `body`；无阴影。
状态：hover 底色加深一档（150ms）；active 再深一档、无位移缩放；disabled 见 §25；loading = 文字旁 `m-sampling` 指示点，禁止按钮变骨架。

## 11. Input

- 适用：文本框、textarea、下拉（Dropdown）、搜索框——同一规格。
- 底 `surface-2`，1px `line` 边框，圆角 `r-sm`，高度 32（textarea 自适应），内边距 8×12，字号 `body`，占位符 `ink-3`。
- focus：边框变 `measure` + Focus Ring（§23）；error：边框 `alert` + 下方 `micro` 档错误原文（等宽），禁止只靠红边不给原因。
- 输入内的读数/参数值用等宽。
- 运行中锁定态 = disabled 规格 + 锁定原因文字，禁止仅降透明度不解释。

## 12. Card（只允许一种）

- 唯一规格：`surface` 底 + 1px `line` 边框 + `r-md` 圆角 + 16 内边距 + **无阴影**。
- 结构（自上而下）：丝印标签行（`label` 档等宽：部件名 · 数据来源）→ 内容 → 可选脚注（`micro`）。
- 禁止：卡片嵌卡片（内部分区用 1px `line` 分隔线）、彩色卡、渐变卡、玻璃卡、阴影卡。
- 失败态：左侧 2px `alert` 内嵌指示条（唯一允许的非 1px 描边例外），内容含错误原文。

## 13. Badge（只允许一种）

- 唯一规格：`r-sm` 圆角（非胶囊）、`micro` 档等宽文字、4×8 内边距、语义色 10% 弱底 + 同色文字、无边框。
- 语义只有四种：中性（`ink-2`）、测量（`measure`）、警示（`caution`）、失败（`alert`）。
- 徽章是**状态读数**，不是装饰：必须由真实字段驱动（如「高熵 0.82」），禁止营销性徽章（New/Hot）。

## 14. Tooltip

- `surface`（暗室 `surface-2`）底 + 1px `line` + `r-md` + `shadow-raise`；`micro` 档文字，读数部分等宽。
- 延迟 300ms 出现，`m-pointer` 淡入；纯文本，禁止在 Tooltip 内放交互控件。
- 仅承载「学名/完整读数/单位解释」，重要信息不允许只存在于 Tooltip。

## 15. Dialog

- 遮罩：黑 40% 纯色（**无 blur**）；面板：`surface` + 1px `line` + `r-md` + `shadow-float`，宽度 ≤560px（观测型大弹层可用 `container-reading`）。
- 进出用 `m-expand`（透明度 + ≤8px 位移）；焦点圈闭（focus trap），Esc 关闭，关闭后焦点归还触发元素。
- 结构：`title` 标题 → 内容 → 右对齐按钮行（Secondary 在左、Primary/Danger 在右）。
- 每屏同时 ≤1 个 Dialog；禁止 Dialog 套 Dialog。

## 16. Chart

- 数据墨水优先：细线（1.5px）+ 数据点，禁止渐变面积填充、禁止 3D 效果图表。
- 概率 = 水平条形（全站同一组件）；熵/连续量 = `measure` 明度阶；时间轴一律横向向右。
- 网格线 `line` 色、0.5–1px、只保留必要基准线；轴标签 `label` 档、读数等宽。
- 每张图必备三件：单位与轴、来源丝印（「本机 trace 实录 · Top-N 截断」）、一步下钻入口。
- 多系列 ≤3，用明度+线型区分；hover 十字标线 + 等宽读数条，`m-pointer` 响应。
- 3D 观测视图为选配入口，服从同一色板，无氛围光、无霓虹材质。

## 17. Empty State

- 诚实缺席三要素：**缺什么字段、为什么缺、（如有）怎么获得**。例：「此记录无 attention 张量 · 当前 runtime 未导出 · 重导出 ONNX 后可见」。
- 视觉：`ink-3` 线性图标（20px）+ `body` 一句说明 + 可选 Secondary 动作；居中，不画插画、不放吉祥物。
- 唯一虚线语义在此：文件投放区用 1px 虚线 `line-strong` 框。
- 禁止用骨架屏或占位假数据冒充空态。

## 18. Loading

- 有真实进度必须显示真实进度：细进度条（2px，`measure`）+ 等宽百分比/已下载量。
- 无法度量的短等待（<1s 预期）：`m-sampling` 单点指示 + 一句测量式文案（「正在采样 · 第 N 步」「正在加载权重 · 214/380MB」）。
- 禁止拟人文案（「AI 正在思考…」）、禁止全屏 spinner、禁止无限转圈超过 10s 不给解释。

## 19. Skeleton

- **默认不用骨架屏**（与诚实缺席冲突）。唯一允许场景：结构已知、数据必达且 <1s 的列表复载。
- 允许时的规格：`surface-2` 纯色块（**无 shimmer 扫光**——扫光即 Glow），`m-sampling` 节奏整体呼吸一档透明度，形状对应真实布局。
- 超过 1s 必须切换为 §18 的真实进度或空态说明。

## 20. Scroll

- 细滚动条：6px，滑块 `ink` 12%（hover 24%），轨道透明（现有做法保留并 token 化）。
- 滚动容器必须有可见边界（边框或底色差），禁止无提示的隐藏横向滚动。
- 长流自动跟随（如计划流式区）：用户向上滚动即暂停跟随，出现「回到最新」Ghost 按钮。
- 禁止 scroll-jacking（劫持滚轮做动画叙事）。

## 21. Focus Ring

- 全站唯一焦点样式：`2px solid measure`，offset 2px，圆角随控件。
- 仅 `:focus-visible` 触发（鼠标点击不出现）；任何可交互元素必须可见此环，禁止 `outline: none` 裸奔。
- 暗室下同规格（`measure` 暗室值）。

## 22. Hover

- 唯一反馈语言：**底色变化一档**（透明 → `surface-2` / 已有底加深一档），150ms `m-pointer`。
- 可点数据对象（token、图表点）：附加 1px `measure` 描边或下划线。
- 禁止 hover 缩放、位移、发光、阴影加深。

## 23. Active（按下/选中）

- 按下：底色再深一档，无位移无缩放。
- 选中（当前被测对象）：`measure-wash` 弱底 + 1px `measure` 边框——全站唯一选中语言，token、列表行、图表点、Tab 通用。
- 同屏「当前被测对象」高亮 ≤1 处主焦点。

## 24. Disabled

- 统一规格：透明度 45% + `cursor: not-allowed`，保留原布局，不改颜色为灰色系以外的值。
- 交互性禁用必须可解释：hover 时 Tooltip 或旁注给出原因（「模型加载中」「运行中已锁定」）。
- 禁止用 disabled 假装功能存在（无数据时应显示空态而不是永久禁用的按钮）。

---

## 25. 合规与迁移

- 效力顺序：数据诚实原则（ACDL）> `design-language.md` > 本文件 > 组件局部约定。
- 现存违规清单（迁移时按序处理，见 design-language.md §7）：临时字号 15+ 种 → 六档；六系彩色 → 三测量色；辉光/粒子/渐变面积/近黑霓虹 → 对应章节规范；胶囊按钮 → `r-sm`。
- 评审四问（新 UI 必答）：像素承载什么证据？属于哪类部件（design-language.md §4 七类）？读数等宽可下钻吗？无数据/失败时长什么样？
